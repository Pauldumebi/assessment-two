import { Box } from '@chakra-ui/react';
import React from 'react';
import './Spinner.css';

const Spinner = () => {
    return (
        <Box className = "loader">
            Loading...
        </Box>
    )
}

export default Spinner;